{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for HackerTerm",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "os:default"
  ]
}
